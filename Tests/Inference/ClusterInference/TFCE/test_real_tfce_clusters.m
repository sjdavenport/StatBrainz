%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%%    This script tests the real_tfce_clusters function
%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% TODO: example inputs are placeholders — verify against intended usage.
% tstat_orig = randn(20, 20);
% mask = ones(20, 20) > 0;
% tfce_threshold = 5;
% [true_tfce_cluster_im, true_tfce_clusters] = real_tfce_clusters(tstat_orig, mask, tfce_threshold);

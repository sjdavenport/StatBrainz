%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%%    This script tests the srf_scb2cope function
%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% TODO: example inputs are placeholders — verify against intended usage.
% srf = loadsrf(...);
% nv = length(srf.vertices);
% lower_band_im = randn(nv, 1);
% upper_band_im = lower_band_im + 0.5;
% muhat = randn(nv, 1);
% c = 0;
% [ lower_set, upper_set, contour, yellow_set ] = srf_scb2cope( srf, lower_band_im, upper_band_im, muhat, c );

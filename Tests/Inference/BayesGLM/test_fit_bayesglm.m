%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%
%%%    This script tests the fit_bayesglm function
%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% TODO: example inputs are placeholders — verify against intended usage.
% BOLD = {randn(100, 50)};
% design = {randn(100, 3)};
% mask_qc.mask = true(50, 1);
% nuisance = [];
% spatial.spatial_type = 'voxel';
% [INLA_model_obj, field_estimates, hyperpar_posteriors] = fit_bayesglm(BOLD, design, mask_qc, nuisance, spatial);
